@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "gridConvenio";
        settings.CallbackRouteValues = new { Controller = "Convenio", Action = "GridViewPartial" };

        settings.SettingsEditing.AddNewRowRouteValues = new { Controller = "Entidad", Action = "GridViewPartialAddNew" };
        settings.SettingsEditing.UpdateRowRouteValues = new { Controller = "Entidad", Action = "GridViewPartialUpdate" };
        settings.SettingsEditing.DeleteRowRouteValues = new { Controller = "Entidad", Action = "GridViewPartialDelete" };
        settings.SettingsEditing.Mode = GridViewEditingMode.EditFormAndDisplayRow;
        settings.SettingsBehavior.ConfirmDelete = true;

        settings.CommandColumn.Visible = false;
        settings.CommandColumn.ShowNewButton = false;
        settings.CommandColumn.ShowDeleteButton = false;
        settings.CommandColumn.ShowEditButton = false;
        settings.CommandColumn.Width = System.Web.UI.WebControls.Unit.Percentage(6);
        //settings.CommandColumn.Caption = "Eliminar";

        settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);

        settings.KeyFieldName = "Id";

        settings.Columns.Add(column =>
        {
            column.Caption = "Convenio";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(5);
            column.SetDataItemTemplateContent(container =>
            {
                Html.DevExpress().HyperLink(hyperlink =>
                {
                    var visibleIndex = container.VisibleIndex;
                    var keyValue = container.KeyValue;
                    var ver = "Ver ";

                    hyperlink.Name = "hl" + keyValue.ToString();
                    hyperlink.Properties.Text = ver.ToString();
                    hyperlink.NavigateUrl = Url.Action("Editar", "Convenio", new { id = keyValue });
                }).Render();
            });

        });

        settings.Columns.Add(column =>
        {
            column.Caption = "Actividades";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(5);
            column.SetDataItemTemplateContent(container =>
            {
                Html.DevExpress().HyperLink(hyperlink =>
                {
                    var visibleIndex = container.VisibleIndex;
                    var keyValue = container.KeyValue;
                    var ver = "Ver actividad";

                    hyperlink.Name = "hactividad" + keyValue.ToString();
                    hyperlink.Properties.Text = ver.ToString();
                    hyperlink.NavigateUrl = Url.Action("Index", "Actividad", new { id = keyValue });
                }).Render();
            });

        });

        settings.Columns.Add(column =>
        {
            column.FieldName = "Nombre";
            column.Caption = "Número";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(10);
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;

        });

        settings.Columns.Add(column =>
        {
            column.FieldName = "Nombreentidad";
            column.Caption = "Entidad";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(25);
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;

        });
        settings.Columns.Add(column =>
        {
            column.FieldName = "Periodo";
            column.Caption = "Periodo";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(7);
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;

        });
        settings.Columns.Add(column =>
        {
            column.FieldName = "Nombreestado";
            column.Caption = "Estado";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(10);
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;

        });

        settings.Columns.Add(column =>
        {
            column.FieldName = "FechaInicio";
            column.Caption = "FechaInicio";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(15);
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;

        });
        settings.Columns.Add(column =>
        {
            column.FieldName = "FechaFin";
            column.Caption = "FechaFin";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(15);
            column.Settings.AutoFilterCondition = AutoFilterCondition.Contains;

        });





        settings.SettingsPager.PageSize = 50;
        settings.SettingsBehavior.FilterRowMode = GridViewFilterRowMode.Auto;

        settings.SettingsPager.Visible = true;
        settings.Settings.ShowGroupPanel = true;
        settings.Settings.ShowFilterRow = true;
        settings.SettingsBehavior.AllowSelectByRowClick = true;
        settings.Settings.ShowFilterRowMenu = true;
        settings.CommandColumn.Visible = true;
        settings.CommandColumn.ShowClearFilterButton = true;
        settings.SettingsPager.PageSizeItemSettings.Visible = true;
        settings.SettingsPager.PageSizeItemSettings.Items = new string[] { "50", "100", "150" };
    });



    if (ViewData["EditError"] != null)
    {
        grid.SetEditErrorText((string)ViewData["EditError"]);
    }
}
@grid.Bind(Model).GetHtml()
