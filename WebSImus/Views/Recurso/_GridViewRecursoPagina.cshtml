
@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "gridRecursoPagina";
        settings.CallbackRouteValues = new { Controller = "Recurso", Action = "GridViewRecursoPagina" };

        settings.SettingsEditing.AddNewRowRouteValues = new { Controller = "Recurso", Action = "GridViewRecursoPaginaAddNew" };
        settings.SettingsEditing.UpdateRowRouteValues = new { Controller = "Recurso", Action = "GridViewRecursoPaginaUpdate" };
        settings.SettingsEditing.DeleteRowRouteValues = new { Controller = "Recurso", Action = "GridViewRecursoPaginaDelete" };
        settings.SettingsEditing.Mode = GridViewEditingMode.EditFormAndDisplayRow;
        settings.SettingsBehavior.ConfirmDelete = true;

        settings.CommandColumn.Visible = false;
        settings.CommandColumn.ShowNewButton = false;
        settings.CommandColumn.ShowDeleteButton = false;
        settings.CommandColumn.ShowEditButton = false;
        settings.CommandColumn.Width = System.Web.UI.WebControls.Unit.Percentage(6);
        settings.CommandColumn.Caption = "";

        settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);

        settings.KeyFieldName = "id";

        settings.Columns.Add(column =>
        {
            column.Caption = "";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(5);
            column.SetDataItemTemplateContent(container =>
            {
                Html.DevExpress().HyperLink(hyperlink =>
                {
                    var visibleIndex = container.VisibleIndex;
                    var keyValue = container.KeyValue;
                    var NombreEscuela = "Eliminar";

                    hyperlink.Name = "hl" + keyValue.ToString();
                    hyperlink.Properties.Text = NombreEscuela.ToString();
                    hyperlink.NavigateUrl = Url.Action("ModificardeletePagina", "Recurso", new { id = keyValue });
                }).Render();
            });

        });



        settings.Columns.Add(column =>
        {
            column.FieldName = "codigo";
            column.Caption = "Codigo";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(34);

        });

        settings.Columns.Add(column =>
        {
            column.FieldName = "rec_nombre";
            column.Caption = "Nombre";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(15);

        });
        settings.Columns.Add(column =>
        {
            column.FieldName = "rec_titulo";
            column.Caption = "Titulo";
            column.Width = System.Web.UI.WebControls.Unit.Percentage(15);

        });

     



        settings.SettingsPager.PageSize = 20;
        settings.SettingsBehavior.FilterRowMode = GridViewFilterRowMode.Auto;

        settings.SettingsPager.Visible = true;
        settings.Settings.ShowGroupPanel = true;
        settings.Settings.ShowFilterRow = true;
        settings.SettingsBehavior.AllowSelectByRowClick = true;
        settings.Settings.ShowFilterRowMenu = true;
        settings.CommandColumn.Visible = true;
        settings.CommandColumn.ShowClearFilterButton = true;
        settings.SettingsPager.PageSizeItemSettings.Visible = true;
        settings.SettingsPager.PageSizeItemSettings.Items = new string[] { "20", "30", "50" };
    });



    if (ViewData["EditError"] != null)
    {
        grid.SetEditErrorText((string)ViewData["EditError"]);
    }
}
@grid.Bind(Model).GetHtml()